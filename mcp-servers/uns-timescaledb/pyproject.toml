[project]
name = "uns-timescaledb"
version = "0.1.0"
description = "MCP server for querying the UNS TimescaleDB historian"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.27.0",
    "asyncpg>=0.30.0",
]

[project.scripts]
uns-timescaledb = "src.server:main"
